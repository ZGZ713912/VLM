# 数据集问题解决方案

## 当前问题分析

### Avenue数据集标注问题
根据项目代码分析，Avenue数据集中的`*.mat`标注文件存在以下问题：
- 文件中的`vol`变量实际上是降采样的灰度视频帧
- 与真实二值异常mask的相关系数≈0.999（几乎完全相关）
- 真实的二值mask应该只有0和255两个值，而灰度帧有上百个不同值

### 项目已提供的解决方案
代码已经包含检测机制：
- `mask_looks_like_frames()`函数可以识别可疑标注
- 支持切换到`frame_label_mode: motion_diff`使用运动伪标签
- 运动伪标签是VAD的经典baseline方法

## 解决方案选项

### 方案1：获取真实Avenue标注（推荐优先尝试）
**步骤：**
1. 访问官方数据集页面：http://www.cse.cuhk.edu.hk/leojia/projects/detectabnormal/dataset.html
2. 下载真实的二值标注文件（training_vol/和testing_vol/目录）
3. 替换当前data目录下的.mat文件

**验证方法：**
- 使用代码中的检测函数验证新标注
- 真实二值mask应该只有0和255两个值
- 大部分区域应该是0，只有小部分异常区域为255

### 方案2：使用运动伪标签（快速验证方案）
**配置修改：**
```yaml
frame_label_mode: motion_diff
```

**优点：**
- 立即可用，无需等待新标注
- 运动差分是经典VAD方法
- 可以快速验证框架可行性

**缺点：**
- 运动伪标签不是真实异常标注
- 学术严谨性稍差

### 方案3：切换到其他数据集（备选方案）

#### UCF-Crime数据集
- **特点**：更大的数据集，多种异常类型
- **标注**：视频级标签（非像素级）
- **适用性**：适合视频级异常检测
- **下载**：https://www.crcv.ucf.edu/chenchen/datasets/

#### ShanghaiTech Campus数据集
- **特点**：人群异常检测，像素级标注
- **适用性**：适合人群异常场景
- **优势**：标注质量高

## 推荐实施顺序

### 阶段1：快速验证（1-2天）
1. **立即执行**：切换到`motion_diff`模式
2. **运行zero-shot测试**：验证框架可行性
3. **生成基础结果**：为中期答辩准备初步数据

### 阶段2：数据集优化（3-5天）
1. **尝试获取真实Avenue标注**
2. **如果成功**：切换回pixel模式，重新训练
3. **如果失败**：考虑UCF-Crime数据集

### 阶段3：数据集扩展（中期后）
1. **多数据集对比实验**
2. **跨数据集泛化性测试**

## 配置文件模板

### 使用运动伪标签的配置
```yaml
data:
  frame_label_mode: motion_diff  # 使用运动差分伪标签
  # 其他配置保持不变
```

### 使用真实标注的配置
```yaml
data:
  frame_label_mode: pixel  # 使用真实像素级标注
  # 其他配置保持不变
```

## 下一步行动

1. **立即执行**：修改配置文件，使用motion_diff模式
2. **运行测试**：验证zero-shot基础功能
3. **同时进行**：尝试获取真实Avenue标注
4. **准备备选**：研究UCF-Crime数据集的集成方案

## 风险评估

### 低风险方案
- 使用motion_diff模式：完全无风险，立即可用
- 学术严谨性稍低，但适合快速验证

### 中等风险方案
- 尝试获取真实标注：需要时间，但提升学术价值
- 可能遇到数据获取困难

### 高风险方案
- 切换新数据集：需要大量适配工作
- 可能影响项目进度

## 结论建议

**推荐采用混合策略：**
1. **立即**：使用motion_diff模式运行zero-shot测试
2. **并行**：尝试获取真实Avenue标注
3. **备选**：准备UCF-Crime数据集集成方案

这样可以确保：
- 中期答辩有基础结果展示
- 学术严谨性得到保障
- 项目进度可控