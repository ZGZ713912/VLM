BASE_IMAGE=pytorch/pytorch:2.3.1-cuda12.1-cudnn8-runtime
LOCAL_UID=1000
LOCAL_GID=1000
TZ=Asia/Shanghai
SHM_SIZE=16gb
JUPYTER_PORT=8888
TENSORBOARD_PORT=6006

# opencode host mounts (default to $HOME paths; uncomment only to override)
# HOST_OPENCODE_CONFIG=${HOME}/.config/opencode
# HOST_OPENCODE_AUTH=${HOME}/.local/share/opencode/auth.json
# HOST_AGENTS_DIR=${HOME}/.agents
