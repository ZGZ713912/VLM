# Prompt标注方案指南

## 概述

本指南详细说明如何为VLM-VAD（Vision-Language Model for Video Anomaly Detection）项目进行人工prompt标注，包括标注流程、规范和最佳实践。

## 标注目标

为每个视频片段从预定义的prompt组中选择最合适的描述，用于训练和评估视觉语言模型在视频异常检测任务中的表现。

## Prompt类型设计

### 1. Label类型（行为标签）
**特点**：直接描述具体的异常行为
**格式**：`"a {behavior} behavior"`

**标注示例**：
- 正确标注：`"a fighting behavior"`
- 正确标注：`"a theft behavior"`
- 正确标注：`a vandalism behavior`
- 错误标注：`"a person running"`（正常行为）

**适用场景**：
- 明确的异常行为（如斗殴、盗窃、破坏）
- 可识别的具体动作
- 有明确语义的行为描述

### 2. Scene类型（场景描述）
**特点**：描述场景环境和异常活动
**格式**：`"a {scene} with abnormal activity"`

**标注示例**：
- 正确标注：`"a street with abnormal activity"`
- 正确标注：`"a mall with abnormal activity"`
- 正确标注：`"a bank with abnormal activity"`
- 错误标注：`"a empty street"`（正常场景）

**适用场景**：
- 特定环境中的异常
- 场景上下文重要的情况
- 需要环境信息来理解异常

### 3. Contrast类型（对比描述）
**特点**：对比正常和异常行为
**格式**：`"normal vs abnormal {activity}"`

**标注示例**：
- 正确标注：`"normal vs abnormal behavior"`
- 正确标注：`"normal vs abnormal movement"`
- 正确标注：`"normal vs abnormal action"`
- 错误标注：`"abnormal behavior"`（缺少正常对比）

**适用场景**：
- 需要突出异常相对正常的行为
- 强调异常的对比性
- 适合需要相对理解的场景

## 标注流程

### 阶段1：准备阶段
1. **熟悉数据集**
   - 观看标注视频，理解内容和背景
   - 了解数据集中的常见异常类型
   - 确认视频的时间范围和异常发生位置

2. **准备标注工具**
   - 使用视频播放器支持逐帧查看
   - 准备标注界面（推荐使用Web界面）
   - 确保标注环境无干扰

### 阶段2：标注阶段
1. **观看视频**
   - 完整观看整个视频片段
   - 标记出异常发生的时间段
   - 记录异常类型和严重程度

2. **选择prompt**
   - 根据异常类型选择最合适的prompt类型
   - 从对应类型的选项中选择最准确的描述
   - 如果现有prompt不合适，可以提出新的候选prompt

3. **质量检查**
   - 检查选择的prompt是否准确描述异常
   - 确保prompt与视频内容匹配
   - 验证标注的一致性

### 阶段3：验证阶段
1. **交叉验证**
   - 多人对同一视频进行标注
   - 比较标注结果的一致性
   - 解决标注分歧

2. **专家审核**
   - 邀请领域专家审核标注结果
   - 确保标注的准确性和合理性
   - 根据专家反馈调整标注策略

## 标注规范

### 基本原则
1. **准确性**：prompt必须准确描述视频中的异常内容
2. **一致性**：相同类型的异常应使用相似的prompt描述
3. **完整性**：每个视频片段都必须有对应的标注
4. **客观性**：基于视频内容进行标注，避免主观判断

### 标注标准
- **异常判定**：偏离正常行为模式的事件
- **时间范围**：异常发生的具体时间段
- **严重程度**：异常的严重程度（轻微、中等、严重）
- **类型分类**：异常的具体类型（暴力、盗窃、破坏等）

### 错误标注示例
1. **过度标注**：
   - 错误：将正常行为标记为异常
   - 正确：只标记真正的异常行为

2. **标注不足**：
   - 错误：忽略明显的异常行为
   - 正确：标记所有明显的异常行为

3. **类型错误**：
   - 错误：将斗殴标记为盗窃
   - 正确：根据实际行为类型选择合适的prompt

## 标注质量控制

### 质量检查指标
1. **标注一致性**：不同标注者之间的 agreement rate
2. **准确性**：标注与真实异常的匹配度
3. **完整性**：所有异常都被正确标注
4. **合理性**：prompt选择符合语义逻辑

### 质量提升方法
1. **标注培训**：对标注者进行培训，统一标注标准
2. **定期审核**：定期抽查标注结果，确保质量
3. **反馈机制**：建立标注反馈机制，及时纠正错误
4. **迭代优化**：根据标注结果优化prompt设计

## 标注数据管理

### 数据结构
```json
{
  "video_id": "video_001",
  "video_path": "data/training_videos/video_001.avi",
  "duration": 30,
  "anomaly_segments": [
    {
      "start_time": 10.5,
      "end_time": 15.2,
      "type": "fighting",
      "severity": "high",
      "prompt": "a fighting behavior",
      "prompt_type": "label",
      "annotator": "user_001",
      "timestamp": "2024-01-01 10:00:00"
    }
  ]
}
```

### 存储格式
- **JSON格式**：结构化存储标注结果
- **数据库存储**：支持大规模标注数据管理
- **版本控制**：标注结果版本管理

## 标注工具推荐

### Web标注界面
1. **视频播放器**：支持逐帧播放、暂停、快进
2. **prompt选择界面**：分类展示不同类型的prompt
3. **标注时间轴**：可视化标注时间范围
4. **质量检查工具**：自动检查标注质量

### 辅助工具
1. **预标注系统**：基于规则自动预标注
2. **相似度匹配**：自动推荐相似的prompt
3. **一致性检查**：自动检查标注一致性
4. **可视化工具**：标注结果可视化

## 标注进度管理

### 分阶段实施
1. **第一阶段**：标注10%的视频样本
2. **第二阶段**：标注30%的视频样本
3. **第三阶段**：标注剩余60%的视频样本

### 进度监控
- **每日进度**：记录每日标注数量
- **质量统计**：统计标注质量指标
- **问题跟踪**：跟踪和解决标注问题

## 成本估算

### 人力资源
- **标注人员**：每人每天可标注20-30个视频
- **审核人员**：每人每天可审核50-100个标注
- **专家**：每周审核10-20个复杂案例

### 时间估算
- **100个视频**：2-3人团队需要1周
- **1000个视频**：5人团队需要2-3周
- **10000个视频**：10人团队需要4-6周

## 风险管理

### 主要风险
1. **标注质量不一致**
2. **标注进度延误**
3. **人员流动导致标注中断**
4. **数据隐私和安全问题**

### 风险缓解
1. **标准化流程**：建立详细的标注规范
2. **备份机制**：定期备份标注数据
3. **培训体系**：完善的标注人员培训
4. **质量监控**：实时监控标注质量

## 总结

人工prompt标注是VLM-VAD项目的重要环节，直接影响模型性能和实验结果。通过建立标准化的标注流程、严格的质量控制和有效的管理机制，可以确保标注数据的高质量，为模型训练和评估提供可靠的基础。

建议：
1. 先进行小规模试点，验证标注流程
2. 逐步扩大标注规模，确保质量
3. 持续优化标注规范和工具
4. 建立长期的质量监控机制